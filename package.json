{
  "name": "blog-ssg",
  "version": "1.0.0",
  "description": "A static site generator for blogs using EJS templates",
  "main": "generate.js",
  "scripts": {
    "build": "node generate.js",
    "dev": "node server.js",
    "start": "npm run build && npm run dev",
    "deploy": "npm run build && gh-pages -d build"
  },
  "keywords": [
    "blog",
    "ssg",
    "static-site-generator"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "ejs": "^3.1.9",
    "express": "^4.18.2",
    "gray-matter": "^4.0.3",
    "markdown-it": "^13.0.2"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
