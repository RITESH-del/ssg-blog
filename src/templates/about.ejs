<!DOCTYPE html>
<html lang="<%= config.site.lang %>">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>About | <%= config.site.title %></title>
    <meta name="description" content="Learn more about <%= config.site.title %> and its author">
    <link rel="stylesheet" href="/styles/main.css">
</head>
<body>
    <nav class="navbar">
        <div class="container">
            <div class="nav-brand">
                <a href="/">✍️ <%= config.site.title %></a>
            </div>
            <ul class="nav-links">
                <% config.navigation.filter(item => item.active).forEach(item => { %>
                    <li><a href="<%= item.path %>" class="<%= item.path === '/about/' ? 'active' : '' %>"><%= item.name %></a></li>
                <% }) %>
            </ul>
        </div>
    </nav>

    <main>
        <section class="about-section">
            <div class="container-small">
                <h1>About <%= config.site.title %></h1>
                
                <div class="about-content">
                    <p><%= config.site.description %></p>
                    
                    <h2>About the Author</h2>
                    <p><strong><%= config.site.author %></strong></p>
                    <p>This is where you can write about yourself, your background, interests, and why you started this blog.</p>

                    <h2>Tech Stack</h2>
                    <ul>
                        <li><strong>Node.js</strong> - JavaScript runtime</li>
                        <li><strong>Express</strong> - Development server</li>
                        <li><strong>EJS</strong> - Templating engine</li>
                        <li><strong>Markdown-it</strong> - Markdown parsing</li>
                        <li><strong>Gray-matter</strong> - Front matter parsing</li>
                    </ul>

                    <h2>Features</h2>
                    <ul>
                        <li>Static site generation for fast performance</li>
                        <li>Markdown support for easy content creation</li>
                        <li>Clean and responsive design</li>
                        <li>SEO-friendly HTML structure</li>
                        <li>Easy to deploy anywhere</li>
                        <li>Configurable via config.js</li>
                    </ul>

                    <% if (config.social.twitter || config.social.github || config.social.linkedin || config.site.email) { %>
                        <h2>Connect</h2>
                        <ul>
                            <% if (config.site.email) { %><li>Email: <a href="mailto:<%= config.site.email %>"><%= config.site.email %></a></li><% } %>
                            <% if (config.social.twitter) { %><li><a href="<%= config.social.twitter %>" target="_blank">Twitter</a></li><% } %>
                            <% if (config.social.github) { %><li><a href="<%= config.social.github %>" target="_blank">GitHub</a></li><% } %>
                            <% if (config.social.linkedin) { %><li><a href="<%= config.social.linkedin %>" target="_blank">LinkedIn</a></li><% } %>
                        </ul>
                    <% } %>

                    <div class="cta">
                        <a href="/" class="button">View All Posts</a>
                    </div>
                </div>
            </div>
        </section>
    </main>

    <footer class="footer">
        <div class="container">
            <p>&copy; <%= new Date().getFullYear() %> <%= config.site.title %>. Built with Node.js & EJS SSG.</p>
        </div>
    </footer>

    <script src="/scripts/main.js"></script>
</body>
</html>
